#include <stdio.h>

int mainProcessamento()
{

    // int x, y;

    // int x;
    // double y;

    // double b1, b2, h, area;

    // int a, b, resultado;

    double a;
    int b;

    // x = 5;
    // y = 2 * x;

    // b1 = 6.0;
    // b2 = 8.0;
    // h = 5.0;
    // area = (b1 + b2) / 2.0 * h;

    // a = 5;
    // b = 2;
    // resultado = a / b;

    a = 5.2;
    b = (int) a;

    // printf("%d\n", x);
    // printf("%d\n", y);

    // printf("%.1lf\n", y);

    // printf("%lf\n", area);

    // printf("%d\n", resultado);

    printf("%d\n", b);

    return 0;
}
